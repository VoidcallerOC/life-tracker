# Copy to .env for local development. Do NOT commit a real production value.

# Shared password protecting every route.
AUTH_PASSWORD=change-me

# Production canonical Vercel Blob store. Private is the safe default; set
# BLOB_ACCESS_MODE only when the connected store is public.
# LIFE_TRACKER_BLOB_READ_WRITE_TOKEN=
# BLOB_ACCESS_MODE=private
